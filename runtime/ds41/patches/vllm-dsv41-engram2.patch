diff --git a/vllm/_custom_ops.py b/vllm/_custom_ops.py
index db20adb..9c6f458 100644
--- a/vllm/_custom_ops.py
+++ b/vllm/_custom_ops.py
@@ -2,6 +2,7 @@
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 
 from enum import IntEnum
+import os
 from typing import TYPE_CHECKING, Literal
 
 import torch
@@ -2431,6 +2432,32 @@ def topk_hash_softplus_sqrt(
     bias_vl: torch.Tensor | None = None,
     image_sentinel_lo: int = 0,
 ) -> None:
+    if os.environ.get("DS41_MOE_C_LEGACY_TEXT_ABI", "0") == "1":
+        # The qualified ROCm extension reused by DS41 predates V4.1's two
+        # vision-routing arguments.  Its first ten arguments implement the
+        # same hash/text route.  Fail closed if an actual image-sentinel token
+        # reaches this language-only runtime instead of silently ignoring VL
+        # bias semantics.
+        if bias_vl is not None and image_sentinel_lo > 0 and input_tokens is not None:
+            has_image = ((input_tokens >= image_sentinel_lo) & (input_tokens < image_sentinel_lo + 5)).any()
+            if bool(has_image.item()):
+                raise RuntimeError(
+                    "DS41 legacy MoE ABI cannot route image-sentinel tokens"
+                )
+        torch.ops._moe_C.topk_softplus_sqrt(
+            topk_weights,
+            topk_indices,
+            token_expert_indices,
+            gating_output,
+            renormalize,
+            routed_scaling_factor,
+            e_score_correction_bias,
+            input_tokens,
+            hash_indices_table,
+            is_padding,
+        )
+        return
+
     torch.ops._moe_C.topk_softplus_sqrt(
         topk_weights,
         topk_indices,
diff --git a/vllm/config/speculative.py b/vllm/config/speculative.py
index ae56e58..b115ca9 100644
--- a/vllm/config/speculative.py
+++ b/vllm/config/speculative.py
@@ -1289,7 +1289,11 @@ class SpeculativeConfig:
                     enforce_eager=self.target_model_config.enforce_eager,
                     max_logprobs=self.target_model_config.max_logprobs,
                     hf_overrides=draft_hf_overrides,
-                    config_format=self.target_model_config.config_format,
+                    config_format=(
+                        "auto"
+                        if self.method == "dspark" and self.draft_load_config is not None
+                        else self.target_model_config.config_format
+                    ),
                 )
 
                 # Old-format Medusa checkpoints (e.g. FasterDecoding/medusa-*)
@@ -1436,10 +1440,21 @@ class SpeculativeConfig:
                         draft_hf_config.n_predict = getattr(
                             draft_hf_config, "dspark_block_size", None
                         ) or getattr(draft_hf_config, "n_predict", None)
-                    self.draft_model_config.quantization = (
-                        self.target_model_config.quantization
-                    )
+                    # When DSpark weights come from the target checkpoint, inherit
+                    # the target quantization. A separate draft_load_config denotes
+                    # an explicit draft sidecar and must preserve its own native
+                    # quantization (for DS41: FP8/FP4 safetensors vs GGUF target).
+                    if self.draft_load_config is None:
+                        self.draft_model_config.quantization = (
+                            self.target_model_config.quantization
+                        )
                     self.update_arch_()
+                    # hf_config_override initially presents DeepSeek drafts as
+                    # deepseek_mtp, so ModelConfig cannot select the V4/V4.1
+                    # expert-aware FP8 config during construction. Re-resolve
+                    # after DSpark restores the real DeepSeek model_type/arch.
+                    if self.draft_load_config is not None:
+                        self.draft_model_config._verify_quantization()
                 elif (
                     self.method == "dspark"
                     and "Gemma4DSparkModel" in self.draft_model_config.architectures
diff --git a/vllm/engine/arg_utils.py b/vllm/engine/arg_utils.py
index db39843..5e9a82a 100644
--- a/vllm/engine/arg_utils.py
+++ b/vllm/engine/arg_utils.py
@@ -2046,6 +2046,7 @@ class EngineArgs:
                     model=self.model,
                     tokenizer=self.tokenizer,
                     revision=self.revision,
+                    config_path=self.hf_config_path,
                     trust_remote_code=self.trust_remote_code,
                     vllm_speculative_config=self.speculative_config,
                     hf_token=self.hf_token,
diff --git a/vllm/entrypoints/generate/base/protocol.py b/vllm/entrypoints/generate/base/protocol.py
index 76dc5e4..76c5c82 100644
--- a/vllm/entrypoints/generate/base/protocol.py
+++ b/vllm/entrypoints/generate/base/protocol.py
@@ -80,6 +80,13 @@ class PerRequestMetrics(OpenAIBaseModel):
     queue_time_ms: float | None = None
     mean_itl_ms: float | None = None
     tokens_per_second: float | None = None
+    prefill_engine_ms: float | None = None
+    prefill_engine_tokens_per_second: float | None = None
+    prompt_tokens_computed: int | None = None
+    prompt_tokens_cached: int | None = None
+    prompt_tokens_local_cache: int | None = None
+    prompt_tokens_external_cache: int | None = None
+    prompt_tokens_cache_creation: int | None = None
     # Experimental, subject to change.
     speculative_decoding: SpeculativeDecodingMetrics | None = None
 
diff --git a/vllm/entrypoints/generate/base/serving.py b/vllm/entrypoints/generate/base/serving.py
index dc41d3f..390bae0 100644
--- a/vllm/entrypoints/generate/base/serving.py
+++ b/vllm/entrypoints/generate/base/serving.py
@@ -93,12 +93,23 @@ def build_per_request_timing_metrics(
         if inference_time_ms > 0:
             tokens_per_second = num_generation_tokens / inference_time_ms * 1000
 
+    prefill_engine_ms = metrics.ds41_prefill_engine_ms
+    prefill_tps = None
+    if prefill_engine_ms is not None and prefill_engine_ms > 0 and metrics.ds41_prompt_tokens_computed is not None:
+        prefill_tps = metrics.ds41_prompt_tokens_computed * 1000 / prefill_engine_ms
     return PerRequestMetrics(
         time_to_first_token_ms=time_to_first_token_ms,
         generation_time_ms=generation_time_ms,
         queue_time_ms=queue_time_ms,
         mean_itl_ms=mean_itl_ms,
         tokens_per_second=tokens_per_second,
+        prefill_engine_ms=prefill_engine_ms,
+        prefill_engine_tokens_per_second=prefill_tps,
+        prompt_tokens_computed=metrics.ds41_prompt_tokens_computed,
+        prompt_tokens_cached=metrics.ds41_prompt_tokens_cached,
+        prompt_tokens_local_cache=metrics.ds41_prompt_tokens_local_cache,
+        prompt_tokens_external_cache=metrics.ds41_prompt_tokens_external_cache,
+        prompt_tokens_cache_creation=metrics.ds41_prompt_tokens_cache_creation,
     )
 
 
diff --git a/vllm/model_executor/kernels/mhc/torch.py b/vllm/model_executor/kernels/mhc/torch.py
index 9c57cc1..8728ec2 100644
--- a/vllm/model_executor/kernels/mhc/torch.py
+++ b/vllm/model_executor/kernels/mhc/torch.py
@@ -1,5 +1,7 @@
 # SPDX-License-Identifier: Apache-2.0
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
+import os
+
 import torch
 
 
@@ -18,25 +20,109 @@ def mhc_pre_delayed_torch(
 ) -> tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]:
     """Reference for mHC pre using coefficients from the previous sublayer."""
     hc_mult = residual.shape[1]
-    x = (residual.flatten(1) if x is None else x).float()
-    mixes = (x @ fn.t()) * torch.rsqrt(x.square().mean(-1, keepdim=True) + rms_eps)
-    pre = (
-        torch.sigmoid(mixes[:, :hc_mult] * hc_scale[0] + hc_base[:hc_mult]) + hc_pre_eps
+    source = residual.flatten(1) if x is None else x
+    projection_requested = os.environ.get("DS41_MHC_PROJECTION_RMS", "0") == "1"
+    ds41_requested = os.environ.get("DS41_MHC_COEFF_SINKHORN", "0") == "1"
+    rowwise_block_requested = (
+        os.environ.get("DS41_MHC_ROWWISE_BLOCK", "0") == "1"
+        and source.ndim == 2
+        and source.shape[0] in (2, 3, 4)
     )
-    post = (
-        torch.sigmoid(
-            mixes[:, hc_mult : 2 * hc_mult] * hc_scale[1]
-            + hc_base[hc_mult : 2 * hc_mult]
+    if rowwise_block_requested and (not projection_requested or not ds41_requested):
+        raise RuntimeError(
+            "DS41 M>1 mHC fidelity control requires promoted projection/RMS "
+            "and coefficient/Sinkhorn paths enabled"
+        )
+    if projection_requested:
+        from runtime.ds41.mhc_projection_rms import (
+            contract as projection_contract,
+            projection_rms_tilelang,
+            record_fallback as projection_record_fallback,
+        )
+
+        projection_ok, projection_reason = projection_contract(source, fn)
+        if projection_ok:
+            mixes = projection_rms_tilelang(source, fn, rms_eps)
+        elif rowwise_block_requested:
+            rows = []
+            for i in range(source.shape[0]):
+                row = source[i : i + 1].contiguous()
+                row_ok, row_reason = projection_contract(row, fn)
+                if not row_ok:
+                    raise RuntimeError(
+                        "DS41 M>1 mHC rowwise projection contract failed: "
+                        f"row={i} reason={row_reason}"
+                    )
+                rows.append(projection_rms_tilelang(row, fn, rms_eps))
+            mixes = torch.cat(rows, dim=0).contiguous()
+        else:
+            projection_record_fallback(projection_reason)
+            xf = source.float()
+            mixes = (xf @ fn.t()) * torch.rsqrt(
+                xf.square().mean(-1, keepdim=True) + rms_eps
+            )
+    else:
+        xf = source.float()
+        mixes = (xf @ fn.t()) * torch.rsqrt(
+            xf.square().mean(-1, keepdim=True) + rms_eps
         )
-        * hc_post_mult_value
+    ds41_fused_coeff = (
+        ds41_requested
+        and (residual.shape[0] == 1 or rowwise_block_requested)
+        and hc_mult == 4
+        and sinkhorn_repeat == 20
+        and mixes.dtype == torch.float32
+        and mixes.is_contiguous()
+        and hc_scale.dtype == torch.float32
+        and hc_base.dtype == torch.float32
     )
-    comb = mixes[:, 2 * hc_mult :].view(-1, hc_mult, hc_mult) * hc_scale[2]
-    comb = comb + hc_base[2 * hc_mult :].view(1, hc_mult, hc_mult)
-    comb = torch.softmax(comb, dim=-1) + hc_sinkhorn_eps
-    comb = comb / (comb.sum(dim=-2, keepdim=True) + hc_sinkhorn_eps)
-    for _ in range(sinkhorn_repeat - 1):
-        comb = comb / (comb.sum(dim=-1, keepdim=True) + hc_sinkhorn_eps)
+    if ds41_fused_coeff:
+        from runtime.ds41.mhc_coeff_sinkhorn import fused_coeff_sinkhorn
+
+        pre, post, comb = fused_coeff_sinkhorn(
+            mixes,
+            hc_scale,
+            hc_base,
+            hc_pre_eps,
+            hc_sinkhorn_eps,
+            hc_post_mult_value,
+            sinkhorn_repeat,
+        )
+    else:
+        if ds41_requested:
+            from runtime.ds41.mhc_coeff_sinkhorn import record_fallback
+
+            if residual.shape[0] != 1:
+                reason = "tokens_not_1"
+            elif hc_mult != 4:
+                reason = "hc_mult_not_4"
+            elif sinkhorn_repeat != 20:
+                reason = "sinkhorn_repeat_not_20"
+            elif mixes.dtype != torch.float32 or not mixes.is_contiguous():
+                reason = "mixes_contract"
+            elif hc_scale.dtype != torch.float32 or hc_base.dtype != torch.float32:
+                reason = "coeff_dtype"
+            else:
+                reason = "unqualified"
+            record_fallback(reason)
+        pre = (
+            torch.sigmoid(mixes[:, :hc_mult] * hc_scale[0] + hc_base[:hc_mult])
+            + hc_pre_eps
+        )
+        post = (
+            torch.sigmoid(
+                mixes[:, hc_mult : 2 * hc_mult] * hc_scale[1]
+                + hc_base[hc_mult : 2 * hc_mult]
+            )
+            * hc_post_mult_value
+        )
+        comb = mixes[:, 2 * hc_mult :].view(-1, hc_mult, hc_mult) * hc_scale[2]
+        comb = comb + hc_base[2 * hc_mult :].view(1, hc_mult, hc_mult)
+        comb = torch.softmax(comb, dim=-1) + hc_sinkhorn_eps
         comb = comb / (comb.sum(dim=-2, keepdim=True) + hc_sinkhorn_eps)
+        for _ in range(sinkhorn_repeat - 1):
+            comb = comb / (comb.sum(dim=-1, keepdim=True) + hc_sinkhorn_eps)
+            comb = comb / (comb.sum(dim=-2, keepdim=True) + hc_sinkhorn_eps)
     layer_input = (
         residual[:, 0]
         if pre_mix is None
diff --git a/vllm/model_executor/layers/fused_moe/expert_map_manager.py b/vllm/model_executor/layers/fused_moe/expert_map_manager.py
index 71f2186..87400ee 100644
--- a/vllm/model_executor/layers/fused_moe/expert_map_manager.py
+++ b/vllm/model_executor/layers/fused_moe/expert_map_manager.py
@@ -349,7 +349,11 @@ class ExpertMapManager:
         if self._expert_map is None:
             return global_id
 
-        return self._expert_map[global_id].item()
+        # Scalar mapping is a host-side weight-loading operation. Never call
+        # .item() on the device routing map here: that would synchronize the
+        # accelerator once per expert checkpoint record.
+        assert self._expert_map_cpu is not None
+        return int(self._expert_map_cpu[global_id])
 
     def is_local_expert(self, global_id: int) -> bool:
         """Check if expert is assigned to this rank."""
@@ -451,6 +455,15 @@ class ExpertMapManager:
             num_fused_shared_experts=self.num_fused_shared_experts,
             return_expert_mask=self.rocm_aiter_enabled,
         )
+        # Weight loading asks for one scalar global->local mapping per expert.
+        # When model construction happens under a GPU default-device context,
+        # _expert_map lives on that device; calling .item() for tens of thousands
+        # of checkpoint records serializes the loader on GPU->CPU synchronizations.
+        # Keep a tiny immutable CPU mirror for scalar loader lookups only. Runtime
+        # routing and kernels continue to use the original device map unchanged.
+        self._expert_map_cpu = (
+            None if self._expert_map is None else self._expert_map.detach().cpu()
+        )
 
         self._local_num_experts += self.num_fused_shared_experts
 
diff --git a/vllm/model_executor/models/utils.py b/vllm/model_executor/models/utils.py
index 6cd2176..3003164 100644
--- a/vllm/model_executor/models/utils.py
+++ b/vllm/model_executor/models/utils.py
@@ -961,8 +961,9 @@ def get_draft_quant_config(vllm_config: VllmConfig) -> "QuantizationConfig | Non
     Returns:
         The draft model's config if available, None otherwise.
     """
-    draft_model_config = vllm_config.speculative_config.draft_model_config
-    draft_load_config = vllm_config.load_config
+    speculative_config = vllm_config.speculative_config
+    draft_model_config = speculative_config.draft_model_config
+    draft_load_config = speculative_config.draft_load_config or vllm_config.load_config
 
     return (
         VllmConfig.get_quantization_config(draft_model_config, draft_load_config)
diff --git a/vllm/models/deepseek_v4_1/amd/dspark.py b/vllm/models/deepseek_v4_1/amd/dspark.py
index 3a6cef8..68179b6 100644
--- a/vllm/models/deepseek_v4_1/amd/dspark.py
+++ b/vllm/models/deepseek_v4_1/amd/dspark.py
@@ -12,6 +12,7 @@ include the future query tokens in the top-k indices for each query token.
 
 from collections.abc import Iterable
 
+import os
 import regex as re
 import torch
 import torch.nn as nn
@@ -59,6 +60,7 @@ logger = init_logger(__name__)
 # MoE expert scale suffix differs by expert dtype (mirrors deepseek_v4 loaders):
 # fp4 experts register ``.weight_scale``; block-fp8 experts ``.weight_scale_inv``.
 _EXPERT_SCALE_RE = re.compile(r"\.experts\.\d+\.w[123]\.scale$")
+_DSPARK_EXPERT_RAW_RE = re.compile(r"mtp\.(\d+)\.ffn\.experts\.(\d+)\.(w[123])\.(weight|scale)$")
 
 
 class DSparkDeepseekV4Model(nn.Module):
@@ -417,6 +419,8 @@ class DSparkDeepseekV4ForCausalLM(nn.Module):
         params_dict = dict(self.named_parameters())
         loaded_params: set[str] = set()
         loaded_confidence_head = False
+        bf16_expert_fallback = os.environ.get("DS41_DSPARK_MXFP4_BF16", "0") == "1"
+        pending_expert_parts: dict[tuple[int, int, str], dict[str, torch.Tensor]] = {}
 
         tp_size = get_tensor_model_parallel_world_size()
         tp_rank = get_tensor_model_parallel_rank()
@@ -425,6 +429,54 @@ class DSparkDeepseekV4ForCausalLM(nn.Module):
         head_end = n_local_head * (tp_rank + 1)
 
         for name, loaded_weight in weights:
+            if bf16_expert_fallback:
+                raw_expert = _DSPARK_EXPERT_RAW_RE.fullmatch(name)
+                if raw_expert is not None:
+                    stage = int(raw_expert.group(1))
+                    expert_id = int(raw_expert.group(2))
+                    proj = raw_expert.group(3)
+                    kind = raw_expert.group(4)
+                    if stage >= len(self.model.layers):
+                        raise RuntimeError(f"invalid DSpark expert stage in {name}")
+                    ffn = self.model.layers[stage].ffn
+                    # EP2 ownership: discard remote expert checkpoint rows before
+                    # any dequantization/copy. Each rank owns a contiguous half.
+                    if not (ffn.experts_start_idx <= expert_id < ffn.experts_end_idx):
+                        continue
+                    key = (stage, expert_id, proj)
+                    parts = pending_expert_parts.setdefault(key, {})
+                    parts[kind] = loaded_weight
+                    if "weight" in parts and "scale" in parts:
+                        raw_weight = parts["weight"]
+                        raw_scale = parts["scale"]
+                        mapped_weight = self._remap_dspark_name(
+                            f"mtp.{stage}.ffn.experts.{expert_id}.{proj}.weight"
+                        )
+                        assert mapped_weight is not None
+                        loaded = False
+                        for param_name, weight_name, mapped_expert_id, shard_id in expert_mapping:
+                            if mapped_expert_id != expert_id or weight_name not in mapped_weight:
+                                continue
+                            name_mapped = mapped_weight.replace(weight_name, param_name)
+                            param = params_dict[name_mapped]
+                            from vllm.utils.flashinfer_moe_ep import _dequant_fp4_ue8m0_gran32
+                            full_weight = _dequant_fp4_ue8m0_gran32(
+                                raw_weight.to(device=param.device, non_blocking=False),
+                                raw_scale.view(torch.uint8).to(device=param.device, non_blocking=False),
+                            )
+                            success = param.weight_loader(
+                                param, full_weight, name_mapped, shard_id=shard_id,
+                                expert_id=expert_id, return_success=True,
+                            )
+                            del full_weight
+                            if success:
+                                loaded_params.add(name_mapped)
+                                loaded = True
+                                break
+                        if not loaded:
+                            raise RuntimeError(f"failed to load local BF16 DSpark expert {key}")
+                        del pending_expert_parts[key]
+                    continue
             mapped = self._remap_dspark_name(name)
             if mapped is None:
                 continue
@@ -499,6 +551,10 @@ class DSparkDeepseekV4ForCausalLM(nn.Module):
                 weight_loader(param, loaded_weight)
                 loaded_params.add(name)
 
+        if bf16_expert_fallback and pending_expert_parts:
+            missing = sorted((k, sorted(v)) for k, v in pending_expert_parts.items())[:8]
+            raise RuntimeError(f"incomplete DSpark FP4 expert weight/scale pairs: {missing}")
+
         if self.model.confidence_head is not None and not loaded_confidence_head:
             self.model.confidence_head = None
         self.process_weights_after_loading()
diff --git a/vllm/models/deepseek_v4_1/amd/rocm.py b/vllm/models/deepseek_v4_1/amd/rocm.py
index 9271e7a..3aa88e1 100644
--- a/vllm/models/deepseek_v4_1/amd/rocm.py
+++ b/vllm/models/deepseek_v4_1/amd/rocm.py
@@ -651,7 +651,15 @@ class DeepseekV41ROCMAiterMLAAttention(DeepseekV4Attention):
         if self._wo_b_scale is not None and zf.dim() == 2:
             result = self._bpre_attn_gemm(self.wo_b.weight, self._wo_b_scale, zf, True)
         else:
-            result = self.wo_b(zf)
+            # DS41 M=1 candidate: replace only the local unquantized
+            # BF16 RowParallel WO_B matmul. The helper validates the exact M=1
+            # TP2 contract and immediately performs the same TP all-reduce;
+            # any contract miss returns None and preserves the original path.
+            from runtime.ds41.attn_wob_llmm1 import try_wob_llmm1
+
+            result = try_wob_llmm1(self.wo_b, zf)
+            if result is None:
+                result = self.wo_b(zf)
         return result
 
     def forward_mqa(
diff --git a/vllm/models/deepseek_v4_1/amd/vl_model.py b/vllm/models/deepseek_v4_1/amd/vl_model.py
index 91a13bd..62bd1a5 100644
--- a/vllm/models/deepseek_v4_1/amd/vl_model.py
+++ b/vllm/models/deepseek_v4_1/amd/vl_model.py
@@ -17,6 +17,7 @@ Thin multimodal wrapper around the text-only ``DeepseekV41LLMForCausalLM``:
 """
 
 from collections.abc import Iterable
+import os
 from typing import Annotated
 
 import torch
@@ -336,14 +337,36 @@ class DeepseekV41ForCausalLM(nn.Module, SupportsMultiModal, SupportsPP, Supports
         return self.language_model.get_mtp_target_hidden_states()
 
     def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:
-        # Map HF names into this wrapper's namespace up front and sort, so
-        # the "language_model." group reaches the child loader as one
-        # contiguous block (AutoWeightsLoader delegates per contiguous group,
-        # and the child's load_weights finalizes fused expert weights, which
-        # must not run on a partially loaded model).
-        mapped = sorted(self.hf_to_vllm_mapper.apply(weights), key=lambda x: x[0])
-        loader = AutoWeightsLoader(self)
-        loaded_params = loader.load_weights(mapped)
+        mapped = self.hf_to_vllm_mapper.apply(weights)
+
+        if os.environ.get("DS41_STREAM_TEXT_WEIGHTS", "0") == "1":
+            # DS41 serves a language-only GGUF.  The outer mapper re-roots every
+            # supported checkpoint tensor under one `language_model.*` group.
+            # Delegate that group to the child exactly once, as a stream, so
+            # its fused-expert finalization still runs only after the complete
+            # text checkpoint has been consumed.  Do not globally sort here:
+            # sorting exhausts the mmap-backed iterator before the first copy
+            # into final parameters and pins the whole source checkpoint in RAM.
+            def language_weights():
+                for name, data in mapped:
+                    prefix, sep, rest = name.partition(".")
+                    if prefix != "language_model" or not sep or not rest:
+                        raise ValueError(
+                            "DS41 streaming loader only accepts the single "
+                            f"language_model group, got {name!r}"
+                        )
+                    yield rest, data
+
+            loaded_child = self.language_model.load_weights(language_weights())
+            loaded_params = {f"language_model.{name}" for name in loaded_child}
+        else:
+            # Generic VL checkpoints can interleave top-level groups.  Preserve
+            # upstream behavior outside the DS41 language-only runtime: sorting
+            # makes each AutoWeightsLoader group contiguous before delegation.
+            sorted_mapped = sorted(mapped, key=lambda x: x[0])
+            loader = AutoWeightsLoader(self)
+            loaded_params = loader.load_weights(sorted_mapped)
+
         # The child's load_weights already ran its post-load finalization.
         self._weights_finalized = True
         return loaded_params
diff --git a/vllm/models/deepseek_v4_1/attention.py b/vllm/models/deepseek_v4_1/attention.py
index 9bb5352..8d9eb7c 100644
--- a/vllm/models/deepseek_v4_1/attention.py
+++ b/vllm/models/deepseek_v4_1/attention.py
@@ -7,6 +7,7 @@ DeepseekV4 MLA Attention Layer
 from abc import ABC, abstractmethod
 from collections.abc import Callable
 from functools import cached_property
+import os
 from typing import TYPE_CHECKING, Any, ClassVar, cast
 
 import regex as re
@@ -16,6 +17,7 @@ import torch.nn.functional as F
 from transformers import DeepseekV2Config, DeepseekV3Config
 
 import vllm.envs as envs
+from vllm import _custom_ops as ops
 from vllm.compilation.breakable_cudagraph import eager_break_during_capture
 from vllm.model_executor.layers.fusion.quant_activation import QuantizedActivation
 from vllm.model_executor.layers.linear import (
@@ -30,6 +32,7 @@ from vllm.models.deepseek_v4_1.common.ops import (
     MXFP4_BLOCK_SIZE,
     fused_indexer_q_rope_quant,
     indexer_k_norm_rope_store,
+    quantize_and_insert_k_cache,
 )
 
 if TYPE_CHECKING:
@@ -72,6 +75,60 @@ from vllm.v1.kv_cache_interface import (
 logger = init_logger(__name__)
 
 
+def _ds41_decomposed_q_rope_kv_insert(
+    q: torch.Tensor,
+    kv: torch.Tensor,
+    k_cache: torch.Tensor,
+    slot_mapping: torch.Tensor,
+    positions: torch.Tensor,
+    cos_sin_cache: torch.Tensor,
+    padded_heads: int,
+    block_size: int,
+) -> torch.Tensor:
+    """V4.1 apply_q_norm=False semantics using already-qualified primitives.
+
+    The reused ROCm `_C` predates the fused op's `apply_q_norm` argument and
+    therefore cannot represent the V4.1 call-site's explicit False value.  Q is
+    already normalized before its projection in V4.1, so applying the legacy
+    kernel's second RMSNorm would be numerically wrong.  Preserve the exact new
+    semantics by splitting the epilogue into: zero-pad Q, GPT-J RoPE on the
+    64-dim tail for Q+KV, then the existing UE8M0 paged-cache insert for KV.
+    """
+    if q.dtype != torch.bfloat16 or kv.dtype != torch.bfloat16:
+        raise RuntimeError("DS41 decomposed Q/KV insert requires bf16 Q and KV")
+    if q.shape[-1] != 512 or kv.shape[-1] != 512:
+        raise RuntimeError("DS41 decomposed Q/KV insert requires head_dim=512")
+    if padded_heads < q.shape[1]:
+        raise RuntimeError("DS41 padded head count is smaller than live Q heads")
+
+    q_out = torch.zeros(
+        (q.shape[0], padded_heads, q.shape[-1]),
+        dtype=q.dtype,
+        device=q.device,
+    )
+    q_out[:, : q.shape[1]].copy_(q)
+    kv_rope = kv.clone()
+    ops.rotary_embedding(
+        positions,
+        q_out,
+        kv_rope,
+        512,
+        cos_sin_cache,
+        False,
+        rope_dim_offset=448,
+        inverse=False,
+    )
+    quantize_and_insert_k_cache(
+        kv_rope,
+        k_cache,
+        slot_mapping,
+        block_size=block_size,
+        is_ue8m0=True,
+        use_fnuz=False,
+    )
+    return q_out
+
+
 def _replace_layer_index(prefix: str, layer_id: int) -> str:
     """Swap the layer index inside a ``...layers.<idx>...`` prefix."""
     new_prefix, n = re.subn(r"\.layers\.\d+\.", f".layers.{layer_id}.", prefix)
@@ -863,11 +920,22 @@ class DeepseekV4Attention(nn.Module, AttentionLayerBase, ABC):
 
         # kv is unchanged; attention reads kv solely via swa_kv_cache.
         if cache_dtype == torch.uint8:
-            # fp8_ds_mla UE8M0 paged path. Horizontally fused:
-            #   Q side: GPT-J RoPE, zero-filling the padding head slots; the
-            #           kernel allocates and returns the padded q tensor.
-            #   KV side: GPT-J RoPE + UE8M0 FP8 quant + paged cache insert.
+            # fp8_ds_mla UE8M0 paged path. Horizontally fused in current V4.1,
+            # but the reused production ROCm `_C` predates `apply_q_norm=False`.
+            # Use a DS41-only decomposition with the same numerical semantics;
+            # never fall back to the legacy fused kernel's unconditional Q norm.
             swa_kv_cache_2d = swa_kv_cache.view(swa_kv_cache.shape[0], -1)
+            if os.environ.get("DS41_DECOMPOSED_QKV_INSERT", "0") == "1":
+                return _ds41_decomposed_q_rope_kv_insert(
+                    q,
+                    kv,
+                    swa_kv_cache_2d,
+                    swa_metadata.slot_mapping,
+                    positions,
+                    cos_sin_cache,
+                    self.padded_heads,
+                    swa_metadata.block_size,
+                )
             return torch.ops._C.fused_deepseek_v4_qnorm_rope_kv_rope_quant_insert(
                 q,
                 kv,
diff --git a/vllm/models/deepseek_v4_1/common/disk_engram.py b/vllm/models/deepseek_v4_1/common/disk_engram.py
new file mode 100644
index 0000000..0ca6b5e
--- /dev/null
+++ b/vllm/models/deepseek_v4_1/common/disk_engram.py
@@ -0,0 +1,249 @@
+# SPDX-License-Identifier: Apache-2.0
+"""DS41 experimental disk-backed 2-bit Engram provider.
+
+Opt-in only through DS41_ENGRAM2_DIR. The n-gram hash/gating remains upstream
+DeepSeek V4.1 code; only the enormous embedding table and its small q/k/wkv
+sidecar are sourced from the verified MLX affine-2bit artifact.
+"""
+from __future__ import annotations
+
+import os
+from pathlib import Path
+
+import numpy as np
+import torch
+from torch import nn
+
+from vllm.distributed import (
+    get_tensor_model_parallel_rank,
+    get_tensor_model_parallel_world_size,
+)
+from vllm.logger import init_logger
+from vllm.triton_utils import triton
+
+from runtime.ds41.affine_safetensors import AffineRowLRU, SafeTensorMMap
+
+logger = init_logger(__name__)
+
+_LAYER_FILE = {
+    1: "model-00047-of-00048.safetensors",
+    14: "model-00048-of-00048.safetensors",
+}
+
+
+def _sidecar_path(root: str | os.PathLike[str], layer_id: int) -> Path:
+    try:
+        name = _LAYER_FILE[layer_id]
+    except KeyError as exc:
+        raise ValueError(f"no DS41 2-bit Engram sidecar mapping for layer {layer_id}") from exc
+    path = Path(root) / name
+    if not path.is_file():
+        raise FileNotFoundError(f"DS41 Engram sidecar is missing: {path}")
+    return path
+
+
+class DiskAffineEngramEmbedding(nn.Module):
+    """TP head-sharded Engram table with affine2 rows read from mmap/SSD."""
+
+    def __init__(
+        self,
+        num_embeddings: int,
+        dim: int,
+        head_sizes: tuple[int, ...],
+        *,
+        layer_id: int,
+        root: str | os.PathLike[str],
+    ) -> None:
+        super().__init__()
+        tp_size = get_tensor_model_parallel_world_size()
+        tp_rank = get_tensor_model_parallel_rank()
+        if not head_sizes or any(size <= 0 for size in head_sizes):
+            raise ValueError("invalid Engram head sizes")
+        if sum(head_sizes) > num_embeddings:
+            raise ValueError("Engram head rows exceed table size")
+        self.num_embeddings = num_embeddings
+        self.dim = dim
+        self.n_hash_cols = len(head_sizes)
+        self.part_n_hash_cols = triton.cdiv(self.n_hash_cols, tp_size)
+        self.head_start = tp_rank * self.part_n_hash_cols
+        head_end = min(self.head_start + self.part_n_hash_cols, self.n_hash_cols)
+        self.local_heads = max(0, head_end - self.head_start)
+        self.vocab_start_idx = sum(head_sizes[: self.head_start])
+        self.vocab_end_idx = sum(head_sizes[:head_end])
+        self.part_num_embeddings = self.vocab_end_idx - self.vocab_start_idx
+        self.tp_size = tp_size
+        self.tp_rank = tp_rank
+        self.layer_id = layer_id
+        self.path = _sidecar_path(root, layer_id)
+        self.source = SafeTensorMMap(self.path)
+        self.base = f"layers.{layer_id}.engram.embed"
+        entry = self.source.entries[self.base + ".weight"]
+        full_shape = (num_embeddings, dim // 16)
+        compact_shape = (self.part_num_embeddings, dim // 16)
+        if entry.shape == full_shape:
+            self.source_row_base = 0
+        elif entry.shape == compact_shape:
+            self.source_row_base = self.vocab_start_idx
+        else:
+            raise ValueError(
+                f"Engram 2-bit shape mismatch for layer {layer_id}: {entry.shape}; "
+                f"expected full={full_shape} or rank-compact={compact_shape}"
+            )
+        max_rows = int(os.environ.get("DS41_ENGRAM_CACHE_ROWS", "65536"))
+        self.cache = AffineRowLRU(self.source, self.base, max_rows=max_rows)
+        self.register_buffer("_anchor", torch.empty(0), persistent=False)
+        logger.info(
+            "DS41 disk Engram layer=%d tp=%d/%d heads=%d..%d rows=%d..%d "
+            "cache_rows=%d path=%s",
+            layer_id,
+            tp_rank,
+            tp_size,
+            self.head_start,
+            head_end,
+            self.vocab_start_idx,
+            self.vocab_end_idx,
+            max_rows,
+            self.path,
+        )
+
+    def lookup(
+        self, indices: torch.Tensor, out: torch.Tensor, background: bool = False
+    ) -> None:
+        del background
+        if indices.ndim != 2 or indices.shape[1] != self.n_hash_cols:
+            raise ValueError(
+                f"Engram ids shape {tuple(indices.shape)} != [T,{self.n_hash_cols}]"
+            )
+        if out.shape != (indices.shape[0], self.part_n_hash_cols, self.dim):
+            raise ValueError(f"Engram output shape mismatch: {tuple(out.shape)}")
+        out.zero_()
+        if not indices.shape[0] or not self.local_heads:
+            return
+        # Hash ids are computed on GPU; one bounded synchronization per Engram
+        # layer turns only this rank's 12 columns into row ids for SSD lookup.
+        local_ids = (
+            indices[:, self.head_start : self.head_start + self.local_heads]
+            .detach()
+            .to("cpu", non_blocking=False)
+            .numpy()
+            .astype(np.int64, copy=False)
+        )
+        if local_ids.size:
+            lo, hi = int(local_ids.min()), int(local_ids.max())
+            if lo < self.vocab_start_idx or hi >= self.vocab_end_idx:
+                raise RuntimeError(
+                    f"Engram hash ownership violation rank={self.tp_rank}: "
+                    f"ids={lo}..{hi} owned={self.vocab_start_idx}..{self.vocab_end_idx}"
+                )
+        rows = self.cache.lookup(local_ids - self.source_row_base)
+        tensor = torch.from_numpy(rows).to(
+            device=out.device, dtype=torch.bfloat16, non_blocking=False
+        )
+        out[:, : self.local_heads].copy_(tensor)
+
+    def forward(self, indices: torch.Tensor) -> torch.Tensor:
+        out = torch.empty(
+            (indices.shape[0], self.part_n_hash_cols, self.dim),
+            dtype=torch.bfloat16,
+            device=indices.device,
+        )
+        self.lookup(indices, out)
+        return out
+
+    def stats(self) -> dict[str, int | str]:
+        return {
+            "layer": self.layer_id,
+            "rank": self.tp_rank,
+            "hits": self.cache.hits,
+            "misses": self.cache.misses,
+            "rows_read": self.cache.rows_read,
+            "path": str(self.path),
+        }
+
+
+class SidecarBF16Linear(nn.Module):
+    """Replicated BF16 linear materialized once from affine2 sidecar weights."""
+
+    def __init__(
+        self,
+        source: SafeTensorMMap,
+        base: str,
+        in_features: int,
+        out_features: int,
+        *,
+        device: torch.device,
+        chunk_rows: int = 256,
+    ) -> None:
+        super().__init__()
+        entry = source.entries[base + ".weight"]
+        if entry.shape[-1] * 16 != in_features or entry.shape[0] != out_features:
+            raise ValueError(
+                f"sidecar linear shape {entry.shape} incompatible with "
+                f"{out_features}x{in_features}"
+            )
+        self.in_features = in_features
+        self.out_features = out_features
+        self.weight = nn.Parameter(
+            torch.empty(
+                out_features,
+                in_features,
+                dtype=torch.bfloat16,
+                device=device,
+            ),
+            requires_grad=False,
+        )
+        with torch.no_grad():
+            for start, end, rows in (
+                (start, end, rows)
+                for start, end, rows in source.affine2_chunks(base, chunk_rows)
+            ):
+                self.weight[start:end].copy_(
+                    torch.from_numpy(rows).to(device=device, dtype=torch.bfloat16)
+                )
+
+    def forward(self, x: torch.Tensor) -> torch.Tensor:
+        return torch.nn.functional.linear(x, self.weight)
+
+
+def load_disk_engram_sidecar(
+    *,
+    root: str | os.PathLike[str],
+    layer_id: int,
+    num_embeddings: int,
+    dim: int,
+    hc_mult: int,
+    head_sizes: tuple[int, ...],
+    device: torch.device,
+):
+    """Create embedding + q/k + dequantized WKV for one V4.1 Engram layer."""
+    path = _sidecar_path(root, layer_id)
+    source = SafeTensorMMap(path)
+    embedding = DiskAffineEngramEmbedding(
+        num_embeddings,
+        256,
+        head_sizes,
+        layer_id=layer_id,
+        root=root,
+    )
+    prefix = f"layers.{layer_id}.engram"
+    q = torch.from_numpy(source.bf16(prefix + ".q_weight")).to(
+        device=device, dtype=torch.bfloat16
+    )
+    k = torch.from_numpy(source.bf16(prefix + ".k_weight")).to(
+        device=device, dtype=torch.bfloat16
+    )
+    expected_qk = (hc_mult, dim)
+    if tuple(q.shape) != expected_qk or tuple(k.shape) != expected_qk:
+        raise ValueError(
+            f"Engram q/k shape mismatch: q={tuple(q.shape)} k={tuple(k.shape)} "
+            f"expected={expected_qk}"
+        )
+    n_hash_cols = len(head_sizes)
+    wkv = SidecarBF16Linear(
+        source,
+        prefix + ".wkv",
+        n_hash_cols * 256,
+        dim * (hc_mult + 1),
+        device=device,
+    )
+    return source, embedding, q, k, wkv
diff --git a/vllm/models/deepseek_v4_1/common/engram.py b/vllm/models/deepseek_v4_1/common/engram.py
index f30c3a5..27d942d 100644
--- a/vllm/models/deepseek_v4_1/common/engram.py
+++ b/vllm/models/deepseek_v4_1/common/engram.py
@@ -34,6 +34,7 @@ chunk-by-chunk while an n-gram at position ``p`` needs the token ids at
   cache for the rest.
 """
 
+import os
 import weakref
 
 import numpy as np
@@ -905,31 +906,55 @@ class Engram(nn.Module):
 
         # Named ``embed_tokens`` so the checkpoint's ``engram.embed.weight``
         # survives the mapper's ``embed.weight`` -> ``embed_tokens.weight``
-        # suffix rule.
-        engram_config = get_current_vllm_config().engram_config
-        self.embed_tokens = ParallelEngramEmbedding(
-            layout.num_embeddings[layer_hash_index],
-            layout.head_dim,
-            tuple(size for order in layout.primes[layer_hash_index] for size in order),
-            cpu_offload=engram_config.cpu_offload if engram_config else True,
+        # suffix rule. DS41 can instead source the table + q/k/wkv from the
+        # verified affine-2bit sidecar without allocating the 203 GiB FP8 table.
+        disk_root = os.environ.get("DS41_ENGRAM2_DIR")
+        head_sizes = tuple(
+            size for order in layout.primes[layer_hash_index] for size in order
         )
         n_hash_cols = (layout.max_ngram_size - 1) * layout.n_heads
-        self.wkv = ReplicatedLinear(
-            n_hash_cols * layout.head_dim,
-            self.dim * (self.hc_mult + 1),
-            bias=False,
-            quant_config=quant_config,
-            return_bias=False,
-            prefix=f"{prefix}.wkv",
-        )
-        self.q_weight = nn.Parameter(
-            torch.empty(self.hc_mult, self.dim, dtype=torch.bfloat16),
-            requires_grad=False,
-        )
-        self.k_weight = nn.Parameter(
-            torch.empty(self.hc_mult, self.dim, dtype=torch.bfloat16),
-            requires_grad=False,
-        )
+        if disk_root:
+            from .disk_engram import load_disk_engram_sidecar
+
+            layer_id = layout.layer_ids[layer_hash_index]
+            sidecar_source, embed_tokens, q, k, wkv = load_disk_engram_sidecar(
+                root=disk_root,
+                layer_id=layer_id,
+                num_embeddings=layout.num_embeddings[layer_hash_index],
+                dim=self.dim,
+                hc_mult=self.hc_mult,
+                head_sizes=head_sizes,
+                device=torch.accelerator.current_accelerator(),
+            )
+            self._ds41_sidecar_source = sidecar_source
+            self.embed_tokens = embed_tokens
+            self.wkv = wkv
+            self.q_weight = nn.Parameter(q, requires_grad=False)
+            self.k_weight = nn.Parameter(k, requires_grad=False)
+        else:
+            engram_config = get_current_vllm_config().engram_config
+            self.embed_tokens = ParallelEngramEmbedding(
+                layout.num_embeddings[layer_hash_index],
+                layout.head_dim,
+                head_sizes,
+                cpu_offload=engram_config.cpu_offload if engram_config else True,
+            )
+            self.wkv = ReplicatedLinear(
+                n_hash_cols * layout.head_dim,
+                self.dim * (self.hc_mult + 1),
+                bias=False,
+                quant_config=quant_config,
+                return_bias=False,
+                prefix=f"{prefix}.wkv",
+            )
+            self.q_weight = nn.Parameter(
+                torch.empty(self.hc_mult, self.dim, dtype=torch.bfloat16),
+                requires_grad=False,
+            )
+            self.k_weight = nn.Parameter(
+                torch.empty(self.hc_mult, self.dim, dtype=torch.bfloat16),
+                requires_grad=False,
+            )
 
         max_tokens = get_current_vllm_config().scheduler_config.max_num_batched_tokens
         # Keep lookup results alive across breakable graph segments.
diff --git a/vllm/models/deepseek_v4_1/quant_config.py b/vllm/models/deepseek_v4_1/quant_config.py
index c8361b2..6df5640 100644
--- a/vllm/models/deepseek_v4_1/quant_config.py
+++ b/vllm/models/deepseek_v4_1/quant_config.py
@@ -4,6 +4,7 @@
 
 from __future__ import annotations
 
+import os
 from typing import TYPE_CHECKING, cast
 
 from vllm.config import get_current_vllm_config
@@ -214,6 +215,15 @@ class DeepseekV4FP8Config(Fp8Config):
             ):
                 return UnquantizedFusedMoEMethod(layer.moe_config)
             if self.expert_dtype == "fp4":
+                # Strix Halo experimental DSpark-only fallback: the official
+                # MXFP4 backend requires optional triton_kernels/AITER support
+                # that is unavailable on gfx1151 in this pinned environment.
+                # Create BF16 expert parameters directly; DSpark load_weights
+                # dequantizes the immutable MXFP4 checkpoint one local matrix
+                # at a time and the normal unquantized Triton MoE executes it.
+                # Target GGUF never uses this quant config.
+                if os.environ.get("DS41_DSPARK_MXFP4_BF16", "0") == "1":
+                    return UnquantizedFusedMoEMethod(layer.moe_config)
                 if self.moe_quant_algo == "NVFP4":
                     from vllm.model_executor.layers.quantization.modelopt import (
                         ModelOptNvFp4FusedMoE,
diff --git a/vllm/transformers_utils/config.py b/vllm/transformers_utils/config.py
index 93e3ad4..c83b0a3 100644
--- a/vllm/transformers_utils/config.py
+++ b/vllm/transformers_utils/config.py
@@ -690,6 +690,7 @@ def maybe_override_with_speculators(
     revision: str | None = None,
     vllm_speculative_config: dict[str, Any] | None = None,
     hf_token: bool | str | None = None,
+    config_path: str | None = None,
     **kwargs,
 ) -> tuple[str, str | None, dict[str, Any] | None]:
     """
@@ -711,7 +712,7 @@ def maybe_override_with_speculators(
     """
     kwargs["local_files_only"] = huggingface_hub.constants.HF_HUB_OFFLINE
     config_dict, _ = PretrainedConfig.get_config_dict(
-        model,
+        config_path or model,
         revision=revision,
         token=hf_token,
         **without_trust_remote_code(kwargs),
diff --git a/vllm/v1/core/sched/scheduler.py b/vllm/v1/core/sched/scheduler.py
index 8b2a551..a3cda23 100644
--- a/vllm/v1/core/sched/scheduler.py
+++ b/vllm/v1/core/sched/scheduler.py
@@ -2153,6 +2153,9 @@ class Scheduler(SchedulerInterface):
 
             # Get prompt logprobs for this request.
             prompt_logprobs_tensors = prompt_logprobs_dict.get(req_id)
+            ds41_prefill_engine_ms = None
+            if model_runner_output.ds41_prefill_engine_ms is not None:
+                ds41_prefill_engine_ms = model_runner_output.ds41_prefill_engine_ms.get(req_id)
             if should_emit_output:
                 # Add EngineCoreOutput for this Request.
                 outputs[request.client_index].append(
@@ -2177,6 +2180,7 @@ class Scheduler(SchedulerInterface):
                         trace_headers=request.trace_headers,
                         routed_experts=routed_experts,
                         num_nans_in_logits=request.num_nans_in_logits,
+                        ds41_prefill_engine_ms=ds41_prefill_engine_ms,
                     )
                 )
             else:
diff --git a/vllm/v1/engine/__init__.py b/vllm/v1/engine/__init__.py
index 5ae9ee0..3bae504 100644
--- a/vllm/v1/engine/__init__.py
+++ b/vllm/v1/engine/__init__.py
@@ -233,6 +233,9 @@ class EngineCoreOutput(
     # Appended last so `array_like` positional serialization stays compatible.
     spec_decode_metrics: RequestSpecDecodeMetrics | None = None
 
+    # DS41 optional pre-sampling prefill duration from the worker.
+    ds41_prefill_engine_ms: float | None = None
+
     @property
     def finished(self) -> bool:
         return self.finish_reason is not None
diff --git a/vllm/v1/engine/output_processor.py b/vllm/v1/engine/output_processor.py
index 419e3aa..63094f2 100644
--- a/vllm/v1/engine/output_processor.py
+++ b/vllm/v1/engine/output_processor.py
@@ -674,13 +674,18 @@ class OutputProcessor:
 
             if req_state.is_prefilling:
                 if engine_core_output.prefill_stats is not None:
-                    req_state.num_cached_tokens = (
-                        engine_core_output.prefill_stats.num_cached_tokens
-                    )
-                    req_state.num_cache_creation_tokens = (
-                        engine_core_output.prefill_stats.num_cache_creation_tokens
-                    )
+                    ps = engine_core_output.prefill_stats
+                    req_state.num_cached_tokens = ps.num_cached_tokens
+                    req_state.num_cache_creation_tokens = ps.num_cache_creation_tokens
+                    if req_state.stats is not None:
+                        req_state.stats.ds41_prompt_tokens_computed = ps.num_computed_tokens
+                        req_state.stats.ds41_prompt_tokens_cached = ps.num_cached_tokens
+                        req_state.stats.ds41_prompt_tokens_local_cache = ps.num_local_cached_tokens
+                        req_state.stats.ds41_prompt_tokens_external_cache = ps.num_external_cached_tokens
+                        req_state.stats.ds41_prompt_tokens_cache_creation = ps.num_cache_creation_tokens
                 req_state.is_prefilling = False
+            if req_state.stats is not None and engine_core_output.ds41_prefill_engine_ms is not None:
+                req_state.stats.ds41_prefill_engine_ms = engine_core_output.ds41_prefill_engine_ms
 
             if engine_core_output.spec_decode_metrics is not None:
                 req_state.spec_decode_metrics = engine_core_output.spec_decode_metrics
diff --git a/vllm/v1/metrics/stats.py b/vllm/v1/metrics/stats.py
index 24e07e5..f7243b8 100644
--- a/vllm/v1/metrics/stats.py
+++ b/vllm/v1/metrics/stats.py
@@ -236,6 +236,14 @@ class RequestStateStats:
     # Track if this request is corrupted (NaNs in logits)
     is_corrupted: bool = False
 
+    # DS41 optional prompt accounting and pre-sampling engine span.
+    ds41_prefill_engine_ms: float | None = None
+    ds41_prompt_tokens_computed: int | None = None
+    ds41_prompt_tokens_cached: int | None = None
+    ds41_prompt_tokens_local_cache: int | None = None
+    ds41_prompt_tokens_external_cache: int | None = None
+    ds41_prompt_tokens_cache_creation: int | None = None
+
 
 @dataclass
 class FinishedRequestStats:
diff --git a/vllm/v1/outputs.py b/vllm/v1/outputs.py
index e943808..f8af4ef 100644
--- a/vllm/v1/outputs.py
+++ b/vllm/v1/outputs.py
@@ -369,6 +369,9 @@ class ModelRunnerOutput:
     # ``None`` when ``return_sampling_mask`` is off.
     sampling_masks: SamplingMaskLists | None = None
 
+    # DS41 opt-in prefill span, per request, milliseconds; first prefill event to logits-ready event before sampling.
+    ds41_prefill_engine_ms: dict[str, float] | None = None
+
     @staticmethod
     def with_kv_conn_output_only(
         kv_connector_output: KVConnectorOutput | None,
diff --git a/vllm/v1/worker/gpu/model_runner.py b/vllm/v1/worker/gpu/model_runner.py
index 533fcbe..111dbfa 100644
--- a/vllm/v1/worker/gpu/model_runner.py
+++ b/vllm/v1/worker/gpu/model_runner.py
@@ -19,6 +19,7 @@ instead of embedding feature-specific logic directly.
 
 import functools
 import gc
+import os
 import time
 from contextlib import AbstractContextManager
 from copy import deepcopy
@@ -201,6 +202,9 @@ class GPUModelRunner(LoRAModelRunnerMixin):
 
         self.device = device
         self.dtype = self.model_config.dtype
+        self.ds41_prefill_telemetry = os.environ.get("DS41_PREFILL_TELEMETRY", "0") == "1"
+        self._ds41_prefill_start_events: dict[str, torch.cuda.Event] = {}
+        self._ds41_prefill_end_events: dict[str, torch.cuda.Event] = {}
         self.kv_cache_dtype = self.dtype
         if self.cache_config.cache_dtype != "auto":
             # Quantized KV cache.
@@ -1483,6 +1487,19 @@ class GPUModelRunner(LoRAModelRunnerMixin):
             sample_hidden_states = hidden_states[input_batch.logits_indices]
             logits = self.model.compute_logits(sample_hidden_states)
 
+        if self.ds41_prefill_telemetry:
+            stream = torch.cuda.current_stream()
+            for i, req_id in enumerate(global_input_batch.req_ids):
+                before = int(global_input_batch.num_computed_prefill_tokens_np[i])
+                prompt_len = int(global_input_batch.prefill_len_np[i])
+                scheduled = int(global_input_batch.num_scheduled_tokens[i])
+                if (req_id in self._ds41_prefill_start_events
+                        and req_id not in self._ds41_prefill_end_events
+                        and before < prompt_len and before + scheduled >= prompt_len):
+                    ev = torch.cuda.Event(enable_timing=True)
+                    ev.record(stream)
+                    self._ds41_prefill_end_events[req_id] = ev
+
         if grammar_output is not None:
             # Apply grammar bitmask to the logits in-place.
             assert self.structured_outputs_worker is not None
@@ -1588,6 +1605,10 @@ class GPUModelRunner(LoRAModelRunnerMixin):
         valid_dummy_state_slots: bool = False,
     ) -> ModelRunnerOutput | IntermediateTensors | None:
         if not dummy_run:
+            if self.ds41_prefill_telemetry:
+                for req_id in scheduler_output.finished_req_ids:
+                    self._ds41_prefill_start_events.pop(req_id, None)
+                    self._ds41_prefill_end_events.pop(req_id, None)
             # Update the request states.
             self.update_pp_decode_requests()
             self.finish_requests(scheduler_output)
@@ -1663,6 +1684,15 @@ class GPUModelRunner(LoRAModelRunnerMixin):
             input_batch = self.prepare_inputs(
                 scheduler_output, batch_req_state, batch_desc
             )
+            if self.ds41_prefill_telemetry:
+                stream = torch.cuda.current_stream()
+                for i, req_id in enumerate(input_batch.req_ids):
+                    if (input_batch.is_prefilling_np[i]
+                            and int(input_batch.num_computed_prefill_tokens_np[i]) == 0
+                            and req_id not in self._ds41_prefill_start_events):
+                        ev = torch.cuda.Event(enable_timing=True)
+                        ev.record(stream)
+                        self._ds41_prefill_start_events[req_id] = ev
             block_tables, slot_mappings = self.prepare_attn(input_batch)
             # Mamba "align" pre-copy: migrate recurrent state across block
             # boundaries before the forward. Runs only on real batches, and
@@ -1972,6 +2002,21 @@ class GPUModelRunner(LoRAModelRunnerMixin):
             hidden_states, input_batch, grammar_output
         )
 
+        ds41_prefill_engine_ms: dict[str, float] = {}
+        if self.ds41_prefill_telemetry:
+            for req_id in input_batch.req_ids:
+                start_ev = self._ds41_prefill_start_events.get(req_id)
+                end_ev = self._ds41_prefill_end_events.get(req_id)
+                if start_ev is None or end_ev is None:
+                    continue
+                # Exactly once per request. The end event is ordered before
+                # sampler work on the same stream; no per-token synchronization.
+                if not end_ev.query():
+                    end_ev.synchronize()
+                ds41_prefill_engine_ms[req_id] = float(start_ev.elapsed_time(end_ev))
+                del self._ds41_prefill_start_events[req_id]
+                del self._ds41_prefill_end_events[req_id]
+
         if self.pp_handler is not None:
             # Broadcast to non-last PP ranks (handles spec decode multi-token).
             self.pp_handler.broadcast(
@@ -2000,6 +2045,7 @@ class GPUModelRunner(LoRAModelRunnerMixin):
             sampled_token_ids=None,  # type: ignore
             prompt_logprobs_dict=prompt_logprobs_dict,  # type: ignore[arg-type]
             cudagraph_stats=cudagraph_stats,
+            ds41_prefill_engine_ms=(ds41_prefill_engine_ms or None),
         )
         # Start async output copy here so that it can overlap with speculator proposal.
         async_output = AsyncOutput(
diff --git a/vllm/v1/worker/gpu/spec_decode/dspark/utils.py b/vllm/v1/worker/gpu/spec_decode/dspark/utils.py
index 5b13530..32541a1 100644
--- a/vllm/v1/worker/gpu/spec_decode/dspark/utils.py
+++ b/vllm/v1/worker/gpu/spec_decode/dspark/utils.py
@@ -51,8 +51,10 @@ def load_dspark_model(target_model: nn.Module, vllm_config: VllmConfig) -> nn.Mo
         vllm_config.attention_config.backend,
     )
 
+    draft_load_config = speculative_config.draft_load_config or vllm_config.load_config
     draft_vllm_config = replace(
         vllm_config,
+        load_config=draft_load_config,
         parallel_config=replace(
             vllm_config.parallel_config,
             pipeline_parallel_size=1,
@@ -80,7 +82,9 @@ def load_dspark_model(target_model: nn.Module, vllm_config: VllmConfig) -> nn.Mo
 
     with set_model_tag("dspark_head"):
         draft_model = get_model(
-            vllm_config=draft_vllm_config, model_config=draft_model_config
+            vllm_config=draft_vllm_config,
+            model_config=draft_model_config,
+            load_config=draft_load_config,
         )
 
     target_language_model = (
diff --git a/vllm/v1/worker/gpu_model_runner.py b/vllm/v1/worker/gpu_model_runner.py
index 2730416..2a1d8f1 100644
--- a/vllm/v1/worker/gpu_model_runner.py
+++ b/vllm/v1/worker/gpu_model_runner.py
@@ -4,6 +4,7 @@
 import functools
 import gc
 import itertools
+import os
 import threading
 import time
 from collections import defaultdict
@@ -522,6 +523,9 @@ class GPUModelRunner(
         parallel_config = self.parallel_config
         self.device = device
         self.dtype = self.model_config.dtype
+        self.ds41_prefill_telemetry = os.environ.get("DS41_PREFILL_TELEMETRY", "0") == "1"
+        self._ds41_prefill_start_events: dict[str, torch.cuda.Event] = {}
+        self._ds41_prefill_end_events: dict[str, tuple[torch.cuda.Event, int]] = {}
 
         self.check_ep_fault = False
         if parallel_config.data_parallel_size > 1 and self.model_config.is_moe:
@@ -4204,6 +4208,17 @@ class GPUModelRunner(
                 "after execute_model() returns None."
             )
 
+        if self.ds41_prefill_telemetry:
+            for finished_req_id in scheduler_output.finished_req_ids:
+                self._ds41_prefill_start_events.pop(finished_req_id, None)
+                self._ds41_prefill_end_events.pop(finished_req_id, None)
+            stream = torch.cuda.current_stream()
+            for new_req in scheduler_output.scheduled_new_reqs:
+                if new_req.req_id not in self._ds41_prefill_start_events:
+                    ev = torch.cuda.Event(enable_timing=True)
+                    ev.record(stream)
+                    self._ds41_prefill_start_events[new_req.req_id] = ev
+
         # If ngram_gpu is used, we need to copy the scheduler_output to avoid
         # the modification has influence on the scheduler_output in engine core process.
         # The replace is much faster than deepcopy.
@@ -4535,6 +4550,19 @@ class GPUModelRunner(
                 assert broadcasted is not None
                 logits = broadcasted["logits"]
 
+        if self.ds41_prefill_telemetry:
+            stream = torch.cuda.current_stream()
+            for req_id in self.input_batch.req_ids[:num_reqs]:
+                idx = self.input_batch.req_id_to_index[req_id]
+                before = int(self.input_batch.num_computed_tokens_cpu[idx])
+                prompt_len = int(self.input_batch.num_prompt_tokens_cpu[idx])
+                scheduled = int(scheduler_output.num_scheduled_tokens.get(req_id, 0))
+                if (req_id in self._ds41_prefill_start_events and req_id not in self._ds41_prefill_end_events
+                        and before < prompt_len and before + scheduled >= prompt_len):
+                    ev = torch.cuda.Event(enable_timing=True)
+                    ev.record(stream)
+                    self._ds41_prefill_end_events[req_id] = (ev, prompt_len)
+
         self.execute_model_state = ExecuteModelState(
             scheduler_output,
             logits,
@@ -4753,6 +4781,25 @@ class GPUModelRunner(
                 scheduler_output.total_num_scheduled_tokens,
             )
 
+        ds41_prefill_engine_ms: dict[str, float] = {}
+        if self.ds41_prefill_telemetry:
+            for req_id in req_ids_output_copy:
+                pair = self._ds41_prefill_end_events.get(req_id)
+                start_ev = self._ds41_prefill_start_events.get(req_id)
+                if pair is None or start_ev is None:
+                    continue
+                end_ev, _ = pair
+                # Resolve exactly once per request, after sampling/bookkeeping.
+                # This is not a per-token synchronization: it runs only when the
+                # final prompt chunk has recorded the pre-sampling end event.
+                # In the normal path the event is already complete; synchronize
+                # only closes the reporting race observed on real serving.
+                if not end_ev.query():
+                    end_ev.synchronize()
+                ds41_prefill_engine_ms[req_id] = float(start_ev.elapsed_time(end_ev))
+                del self._ds41_prefill_start_events[req_id]
+                del self._ds41_prefill_end_events[req_id]
+
         if draft_after_bookkeeping:
             # ngram and other speculative decoding methods use the sampled
             # tokens on the CPU, so they are run after bookkeeping.
@@ -4800,6 +4847,7 @@ class GPUModelRunner(
                 num_nans_in_logits=num_nans_in_logits,
                 cudagraph_stats=cudagraph_stats,
                 routed_experts=None,
+                ds41_prefill_engine_ms=(ds41_prefill_engine_ms or None),
             )
 
         if not self.use_async_scheduling:
