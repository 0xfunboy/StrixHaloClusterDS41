diff --git a/vllm_gguf_plugin/loader.py b/vllm_gguf_plugin/loader.py
index ddea77f..611259a 100644
--- a/vllm_gguf_plugin/loader.py
+++ b/vllm_gguf_plugin/loader.py
@@ -3,6 +3,7 @@
 from __future__ import annotations
 
 import os
+import time
 from typing import TYPE_CHECKING, NamedTuple, cast
 
 import torch
@@ -204,7 +205,16 @@ class GGUFModelLoader(BaseModelLoader):
         self, vllm_config: VllmConfig, model_config: ModelConfig, prefix: str = ""
     ) -> nn.Module:
         device_config = vllm_config.device_config
+        phase_log = os.environ.get("DS41_LOAD_PHASE_LOG", "0") == "1"
+        prepare_started = time.monotonic()
+        if phase_log:
+            print("DS41_LOAD phase=prepare_adapter begin", flush=True)
         adapter, plan = self._prepare_adapter(model_config)
+        if phase_log:
+            print(
+                f"DS41_LOAD phase=prepare_adapter end elapsed_s={time.monotonic() - prepare_started:.3f}",
+                flush=True,
+            )
         logger.debug("GGUF unquantized modules: %s", plan.unquantized_modules)
         vllm_config.quant_config = cast(GGUFConfig, vllm_config.quant_config)
         vllm_config.quant_config.unquantized_modules.extend(plan.unquantized_modules)
@@ -214,6 +224,9 @@ class GGUFModelLoader(BaseModelLoader):
         )
 
         target_device = torch.device(device_config.device)
+        t0 = time.monotonic()
+        if phase_log:
+            print("DS41_LOAD phase=initialize_model begin", flush=True)
         with set_default_torch_dtype(model_config.dtype):
             with target_device:
                 model = initialize_model(
@@ -226,8 +239,25 @@ class GGUFModelLoader(BaseModelLoader):
                     vllm_config.quant_config,
                     prefix=prefix,
                 )
+            if phase_log:
+                print(
+                    f"DS41_LOAD phase=initialize_model end elapsed_s={time.monotonic() - t0:.3f}; phase=load_weights begin",
+                    flush=True,
+                )
+            t1 = time.monotonic()
             model.load_weights(
                 self._iter_weights(adapter, plan, model_config),
             )
+            if phase_log:
+                print(
+                    f"DS41_LOAD phase=load_weights end elapsed_s={time.monotonic() - t1:.3f}; phase=process_weights begin",
+                    flush=True,
+                )
+            t2 = time.monotonic()
             process_weights_after_loading(model, model_config, target_device)
+            if phase_log:
+                print(
+                    f"DS41_LOAD phase=process_weights end elapsed_s={time.monotonic() - t2:.3f} total_s={time.monotonic() - t0:.3f}",
+                    flush=True,
+                )
         return model
diff --git a/vllm_gguf_plugin/ops.py b/vllm_gguf_plugin/ops.py
index cc3a0d3..0c20438 100644
--- a/vllm_gguf_plugin/ops.py
+++ b/vllm_gguf_plugin/ops.py
@@ -252,17 +252,31 @@ def ggml_moe_a8_vec(
     quant_type: int,
     row: int,
     tokens: int,
+    expert_map: torch.Tensor | None = None,
 ) -> torch.Tensor:
-    if _cuda_kernel_available("ggml_moe_a8_vec", quant_type):
+    # The compiled extension accepts only local expert ids.  The EP skip path
+    # instead keeps global router ids and lets moe_align_block_size discard
+    # remote routes on GPU before the generic Triton kernels read any weights.
+    if expert_map is None and _cuda_kernel_available("ggml_moe_a8_vec", quant_type):
         return torch.ops._C_gguf.ggml_moe_a8_vec(
             X, W, topk_ids, top_k, quant_type, row, tokens
         )
     from vllm.model_executor.layers.fused_moe.fused_moe import moe_align_block_size
 
-    E = W.shape[0]
-    sorted_token_ids, expert_ids, num_tokens_post_padded = moe_align_block_size(
-        topk_ids, get_triton_moe_block_m(quant_type), E
-    )
+    if expert_map is None:
+        num_experts = W.shape[0]
+        sorted_token_ids, expert_ids, num_tokens_post_padded = moe_align_block_size(
+            topk_ids, get_triton_moe_block_m(quant_type), num_experts
+        )
+    else:
+        num_experts = expert_map.numel()
+        sorted_token_ids, expert_ids, num_tokens_post_padded = moe_align_block_size(
+            topk_ids,
+            get_triton_moe_block_m(quant_type),
+            num_experts,
+            expert_map=expert_map,
+            ignore_invalid_experts=True,
+        )
     return ggml_moe_a8_triton(
         X,
         W,
diff --git a/vllm_gguf_plugin/quantization/fused_moe.py b/vllm_gguf_plugin/quantization/fused_moe.py
index 758429b..1297538 100644
--- a/vllm_gguf_plugin/quantization/fused_moe.py
+++ b/vllm_gguf_plugin/quantization/fused_moe.py
@@ -2,12 +2,14 @@
 # SPDX-FileCopyrightText: Copyright contributors to the vLLM project
 
 from functools import partial
+import os
 
 import torch
 from vllm.model_executor.layers.fused_moe import (
     RoutedExperts,
 )
 from vllm.model_executor.layers.fused_moe.activation import (
+    ApplyMoEActivationConfig,
     MoEActivation,
     apply_moe_activation,
 )
@@ -28,10 +30,128 @@ from .params import (
     _gguf_moe_weight_loader,
     _gguf_moe_weight_type_loader,
 )
-from .utils import MMQ_QUANT_TYPES, MMVQ_QUANT_TYPES, logger
+from .utils import MMQ_QUANT_TYPES, MMVQ_QUANT_TYPES, WeightType, logger
 
 
-def _fused_moe_gguf(
+_DS41_NATIVE_HIP_STATS = {
+    "native_calls": 0,
+    "native_tokens": 0,
+    "triton_fallback_calls": 0,
+    "fallback_reasons": {},
+}
+_DS41_NATIVE_HIP_FIRST_LOGGED = False
+
+
+def ds41_native_hip_stats() -> dict:
+    return {
+        "native_calls": int(_DS41_NATIVE_HIP_STATS["native_calls"]),
+        "native_tokens": int(_DS41_NATIVE_HIP_STATS["native_tokens"]),
+        "triton_fallback_calls": int(_DS41_NATIVE_HIP_STATS["triton_fallback_calls"]),
+        "fallback_reasons": dict(_DS41_NATIVE_HIP_STATS["fallback_reasons"]),
+    }
+
+
+def _ds41_native_hip_contract(
+    x: torch.Tensor,
+    w1: torch.Tensor,
+    w2: torch.Tensor,
+    topk_weights: torch.Tensor,
+    topk_ids: torch.Tensor,
+    weight_type: int,
+    weight_type2: int,
+    activation: str,
+    expert_map: torch.Tensor | None,
+) -> tuple[bool, str]:
+    if expert_map is None:
+        return False, "no_expert_map"
+    if x.shape[0] != 1:
+        return False, "tokens_not_1"
+    if x.dtype != torch.bfloat16:
+        return False, "activation_not_bfloat16"
+    if activation != "silu":
+        return False, "activation_not_silu"
+    if int(weight_type) != int(WeightType.IQ2_XXS):
+        return False, "gate_up_not_iq2_xxs"
+    if int(weight_type2) != int(WeightType.Q2_K):
+        return False, "down_not_q2_k"
+    if topk_ids.ndim != 2 or tuple(topk_ids.shape) != (1, 6):
+        return False, "topk_shape_not_1x6"
+    if tuple(topk_weights.shape) != tuple(topk_ids.shape):
+        return False, "topk_weight_shape_mismatch"
+    if not x.is_contiguous():
+        return False, "activation_not_contiguous"
+    if not w1.is_contiguous() or not w2.is_contiguous():
+        return False, "weights_not_contiguous"
+    if w1.ndim != 3 or w2.ndim != 3 or w1.shape[0] != w2.shape[0]:
+        return False, "expert_weight_layout"
+    return True, "qualified"
+
+
+def _fused_moe_gguf_native_hip(
+    x: torch.Tensor,
+    w1: torch.Tensor,
+    w2: torch.Tensor,
+    topk_weights: torch.Tensor,
+    topk_ids: torch.Tensor,
+    weight_type: int,
+    weight_type2: int,
+    activation: str,
+    swiglu_limit: float,
+    expert_map: torch.Tensor,
+) -> torch.Tensor:
+    global _DS41_NATIVE_HIP_FIRST_LOGGED
+    namespace = getattr(torch.ops, "_C_gguf", None)
+    if namespace is None or not hasattr(namespace, "ggml_moe_a8_vec"):
+        raise RuntimeError(
+            "DS41 native HIP M=1 requested but verified _C_gguf is not loaded"
+        )
+    activation_enum = MoEActivation.from_str(activation)
+    activation_config = ApplyMoEActivationConfig(
+        clamp_limit=swiglu_limit if swiglu_limit > 0 else None
+    )
+
+    def act(inp: torch.Tensor):
+        d = inp.shape[-1] // 2
+        out = torch.empty(inp.shape[:-1] + (d,), dtype=inp.dtype, device=inp.device)
+        apply_moe_activation(
+            activation_enum, out, inp, activation_config=activation_config
+        )
+        return out
+
+    local_ids = expert_map[topk_ids.to(torch.long)].to(torch.int32).contiguous()
+    top_k = topk_ids.shape[1]
+    gate_up = torch.ops._C_gguf.ggml_moe_a8_vec(
+        x, w1, local_ids, top_k, int(weight_type), w1.shape[1], 1
+    )
+    activated = act(gate_up)
+    down = torch.ops._C_gguf.ggml_moe_a8_vec(
+        activated,
+        w2,
+        local_ids,
+        1,
+        int(weight_type2),
+        w2.shape[1],
+        top_k,
+    )
+    weighted = down.reshape(1, top_k, w2.shape[1]).mul_(
+        topk_weights.view(1, top_k, 1)
+    )
+    out = torch.empty_like(x)
+    ops.moe_sum(weighted, out)
+    _DS41_NATIVE_HIP_STATS["native_calls"] += 1
+    _DS41_NATIVE_HIP_STATS["native_tokens"] += 1
+    if not _DS41_NATIVE_HIP_FIRST_LOGGED:
+        _DS41_NATIVE_HIP_FIRST_LOGGED = True
+        print(
+            "DS41_NATIVE_HIP_MOE_EXECUTE "
+            f"shape={tuple(x.shape)} dtype={x.dtype} topk={top_k} "
+            f"qtypes={int(weight_type)}/{int(weight_type2)}",
+            flush=True,
+        )
+    return out
+
+
+def _fused_moe_gguf_impl(
     x: torch.Tensor,
     w1: torch.Tensor,
     w2: torch.Tensor,
@@ -40,14 +160,21 @@ def _fused_moe_gguf(
     weight_type: int,
     weight_type2: int,
     activation: str,
+    swiglu_limit: float,
+    expert_map: torch.Tensor | None = None,
 ) -> torch.Tensor:
     activation_enum = MoEActivation.from_str(activation)
+    activation_config = ApplyMoEActivationConfig(
+        clamp_limit=swiglu_limit if swiglu_limit > 0 else None
+    )
 
     def act(inp: torch.Tensor):
         d = inp.shape[-1] // 2
         output_shape = inp.shape[:-1] + (d,)
         out = torch.empty(output_shape, dtype=inp.dtype, device=inp.device)
-        apply_moe_activation(activation_enum, out, inp)
+        apply_moe_activation(
+            activation_enum, out, inp, activation_config=activation_config
+        )
         return out
 
     from vllm.model_executor.layers.fused_moe.fused_moe import moe_align_block_size
@@ -98,11 +225,20 @@ def _fused_moe_gguf(
         E, N, _ = w1.shape
         top_k = topk_ids.shape[1]
 
-        out = ops.ggml_moe_a8_vec(x, w1, topk_ids, top_k, weight_type, N, num_tokens)
+        out = ops.ggml_moe_a8_vec(
+            x, w1, topk_ids, top_k, weight_type, N, num_tokens, expert_map=expert_map
+        )
         out = act(out)
 
         out = ops.ggml_moe_a8_vec(
-            out, w2, topk_ids, 1, weight_type2, w2.shape[1], num_tokens * top_k
+            out,
+            w2,
+            topk_ids,
+            1,
+            weight_type2,
+            w2.shape[1],
+            num_tokens * top_k,
+            expert_map=expert_map,
         )
         out = out.reshape(num_tokens, top_k, w2.shape[1]).mul_(
             topk_weights.view(num_tokens, top_k, 1)
@@ -133,6 +269,31 @@ def _fused_moe_gguf(
     return out_hidden_states
 
 
+def _fused_moe_gguf(
+    x: torch.Tensor,
+    w1: torch.Tensor,
+    w2: torch.Tensor,
+    topk_weights: torch.Tensor,
+    topk_ids: torch.Tensor,
+    weight_type: int,
+    weight_type2: int,
+    activation: str,
+    swiglu_limit: float,
+) -> torch.Tensor:
+    return _fused_moe_gguf_impl(
+        x,
+        w1,
+        w2,
+        topk_weights,
+        topk_ids,
+        weight_type,
+        weight_type2,
+        activation,
+        swiglu_limit,
+        None,
+    )
+
+
 def _fused_moe_gguf_fake(
     x: torch.Tensor,
     w1: torch.Tensor,
@@ -142,8 +303,9 @@ def _fused_moe_gguf_fake(
     weight_type: int,
     weight_type2: int,
     activation: str,
+    swiglu_limit: float,
 ) -> torch.Tensor:
-    del w1, w2, topk_weights, topk_ids, weight_type, weight_type2, activation
+    del w1, w2, topk_weights, topk_ids, weight_type, weight_type2, activation, swiglu_limit
     return torch.empty_like(x)
 
 
@@ -266,6 +428,75 @@ class GGUFMoEMethod(FusedMoEMethodBase):
 
         from . import fused_moe_gguf as fused_moe_gguf_op
 
+        # Under TP+expert-parallel vLLM stores complete experts locally and
+        # exposes a global->local map (-1 for experts owned by the peer).
+        # Keep the router's globally-normalized weights unchanged: each rank
+        # computes only its local contribution, remote routes are zero-weighted,
+        # and the normal TP all-reduce reconstructs the exact routed sum.
+        expert_map = layer.expert_map
+        ep_skip_remote = (
+            expert_map is not None
+            and os.environ.get("DS41_EP_SKIP_REMOTE", "0") == "1"
+            and x.shape[0] <= 64
+            and layer.w13_weight_type.weight_type in MMVQ_QUANT_TYPES
+            and layer.w2_weight_type.weight_type in MMVQ_QUANT_TYPES
+        )
+        if ep_skip_remote:
+            native_requested = os.environ.get("DS41_NATIVE_HIP_MOE", "0") == "1"
+            if native_requested:
+                native_ok, reason = _ds41_native_hip_contract(
+                    x,
+                    layer.w13_weight,
+                    layer.w2_weight,
+                    topk_weights,
+                    topk_ids,
+                    layer.w13_weight_type.weight_type,
+                    layer.w2_weight_type.weight_type,
+                    layer.activation.value,
+                    expert_map,
+                )
+                if native_ok:
+                    return _fused_moe_gguf_native_hip(
+                        x,
+                        layer.w13_weight,
+                        layer.w2_weight,
+                        topk_weights,
+                        topk_ids,
+                        layer.w13_weight_type.weight_type,
+                        layer.w2_weight_type.weight_type,
+                        layer.activation.value,
+                        float(self.moe.swiglu_limit or 0.0),
+                        expert_map,
+                    )
+                _DS41_NATIVE_HIP_STATS["triton_fallback_calls"] += 1
+                reasons = _DS41_NATIVE_HIP_STATS["fallback_reasons"]
+                reasons[reason] = int(reasons.get(reason, 0)) + 1
+
+            # Keep global router ids and globally-normalized weights unchanged.
+            # The pinned vLLM aligner uses expert_map on GPU to omit remote
+            # routes, maps owned experts to local ids, and emits expert=-1 for
+            # unused blocks.  GGUF Triton kernels return before weight reads for
+            # those blocks, including the zero-local-route case.
+            return _fused_moe_gguf_impl(
+                x,
+                layer.w13_weight,
+                layer.w2_weight,
+                topk_weights,
+                topk_ids,
+                layer.w13_weight_type.weight_type,
+                layer.w2_weight_type.weight_type,
+                layer.activation.value,
+                float(self.moe.swiglu_limit or 0.0),
+                expert_map,
+            )
+        if expert_map is not None:
+            local_ids = expert_map[topk_ids.to(torch.long)].to(topk_ids.dtype)
+            remote = local_ids < 0
+            topk_weights = topk_weights.masked_fill(remote, 0)
+            # Correctness-first fallback: remote routes run local expert0 and
+            # are zero-weighted after the down projection.
+            topk_ids = local_ids.clamp_min(0)
+
         return fused_moe_gguf_op(
             x,
             layer.w13_weight,
@@ -275,4 +506,5 @@ class GGUFMoEMethod(FusedMoEMethodBase):
             layer.w13_weight_type.weight_type,
             layer.w2_weight_type.weight_type,
             layer.activation.value,
+            float(self.moe.swiglu_limit or 0.0),
         )
diff --git a/vllm_gguf_plugin/weight_utils.py b/vllm_gguf_plugin/weight_utils.py
index 681e06d..231eadb 100644
--- a/vllm_gguf_plugin/weight_utils.py
+++ b/vllm_gguf_plugin/weight_utils.py
@@ -2,6 +2,7 @@
 
 import glob
 import itertools
+import mmap
 import os
 import re
 from collections.abc import Generator, Iterable
@@ -17,6 +18,20 @@ from vllm.transformers_utils.repo_utils import list_filtered_repo_files
 logger = init_logger(__name__)
 
 
+def _torch_from_numpy_view(array: np.ndarray) -> torch.Tensor:
+    """Create a CPU tensor view without copying GGUF mmap-backed data.
+
+    GGUF tensors can be tens of GiB.  ``torch.tensor(array)`` eagerly clones
+    those bytes into anonymous RAM before the actual vLLM shard loader runs,
+    doubling the load peak.  The loader consumes each yielded tensor
+    synchronously and copies only its local shard to the target device, so a
+    read-only NumPy-backed view is the correct ownership boundary here.
+    """
+    if not array.flags.c_contiguous:
+        raise ValueError(f"GGUF tensor must be C-contiguous for zero-copy load: {array.shape}")
+    return torch.from_numpy(array)
+
+
 def download_gguf(
     repo_id: str,
     quant_type: str,
@@ -176,29 +191,63 @@ def gguf_quant_weights_iterator_multi(
     """
     _QUANT_TYPES = ("F32", "BF16", "F16")
 
+    drop_shard_cache = os.environ.get("DS41_DROP_SHARD_CACHE", "0") == "1"
+    phase_log = os.environ.get("DS41_LOAD_PHASE_LOG", "0") == "1"
+
     for gguf_file in gguf_files:
+        if phase_log:
+            print(f"DS41_LOAD shard=begin path={gguf_file}", flush=True)
         reader = gguf.GGUFReader(gguf_file)
-        for tensor in reader.tensors:
-            if gguf_to_hf_name_map is not None:
-                if tensor.name not in gguf_to_hf_name_map:
-                    continue
-                name = gguf_to_hf_name_map[tensor.name]
-            else:
-                name = tensor.name
-
-            weight_type = tensor.tensor_type
-            if weight_type.name not in _QUANT_TYPES:
-                yield name.replace("weight", "weight_type"), torch.tensor(weight_type)
-
-            weight = tensor.data
-            if weight_type.name == "BF16" and weight.dtype == np.uint8:
-                weight = weight.view(np.uint16)
-                if reader.byte_order == "S":
-                    weight = weight.byteswap()
-                param = torch.tensor(weight).view(torch.bfloat16)
-            else:
-                param = torch.tensor(weight)
-            yield name, param
+        try:
+            for tensor in reader.tensors:
+                if gguf_to_hf_name_map is not None:
+                    if tensor.name not in gguf_to_hf_name_map:
+                        continue
+                    name = gguf_to_hf_name_map[tensor.name]
+                else:
+                    name = tensor.name
+
+                weight_type = tensor.tensor_type
+                if weight_type.name not in _QUANT_TYPES:
+                    yield name.replace("weight", "weight_type"), torch.tensor(weight_type)
+
+                weight = tensor.data
+                if weight_type.name == "BF16" and weight.dtype == np.uint8:
+                    weight = weight.view(np.uint16)
+                    if reader.byte_order == "S":
+                        # Byte-swapped GGUF cannot be represented as a native-endian
+                        # zero-copy torch view; this uncommon path deliberately copies.
+                        weight = weight.byteswap()
+                        param = torch.tensor(weight).view(torch.bfloat16)
+                    else:
+                        param = _torch_from_numpy_view(weight).view(torch.bfloat16)
+                else:
+                    param = _torch_from_numpy_view(weight)
+                yield name, param
+        finally:
+            # On Strix Halo, GPU/GTT allocations and file-backed GGUF pages use
+            # the same physical UMA.  Once every tensor in this shard has been
+            # consumed by model.load_weights(), retaining its clean mmap pages
+            # can block later device allocations even though the checkpoint is
+            # immutable and fully recoverable from disk.  Drop only this shard's
+            # clean pages; never use global drop_caches/swapoff.
+            if drop_shard_cache:
+                mm = getattr(reader.data, "_mmap", None)
+                if mm is None or not hasattr(mm, "madvise") or not hasattr(mmap, "MADV_DONTNEED"):
+                    raise RuntimeError("DS41 shard cache discard requires mmap MADV_DONTNEED")
+                mm.madvise(mmap.MADV_DONTNEED)
+                if not hasattr(os, "posix_fadvise") or not hasattr(os, "POSIX_FADV_DONTNEED"):
+                    raise RuntimeError("DS41 shard cache discard requires POSIX_FADV_DONTNEED")
+                fd = os.open(gguf_file, os.O_RDONLY)
+                try:
+                    os.posix_fadvise(fd, 0, 0, os.POSIX_FADV_DONTNEED)
+                finally:
+                    os.close(fd)
+            if phase_log:
+                print(
+                    f"DS41_LOAD shard=end path={gguf_file} cache_drop={int(drop_shard_cache)}",
+                    flush=True,
+                )
 
 
 def split_stacked_experts(
diff --git a/vllm_gguf_plugin/weights_adapter/__init__.py b/vllm_gguf_plugin/weights_adapter/__init__.py
index be2c1d7..a34d864 100644
--- a/vllm_gguf_plugin/weights_adapter/__init__.py
+++ b/vllm_gguf_plugin/weights_adapter/__init__.py
@@ -3,6 +3,7 @@
 
 from ..gguf_files import GGUFModelFiles
 from .base import BaseGGUFWeightsAdapter
+from .deepseek_v41 import DeepseekV41GGUFAdapter
 from .diffusion import (
     DiffusionGGUFAdapter,
     Flux2KleinDiffusionGGUFAdapter,
@@ -17,6 +18,7 @@ from .qwen3_5 import Qwen35GGUFAdapter, Qwen35MtpGGUFAdapter
 from .transformers import TransformersGGUFWeightsAdapter
 
 _ADAPTER_REGISTRY: list[type[BaseGGUFWeightsAdapter]] = [
+    DeepseekV41GGUFAdapter,
     Gemma3GGUFAdapter,
     Gemma4GGUFAdapter,
     OLMoEGGUFAdapter,
@@ -43,6 +45,7 @@ def get_adapter_architecture(config) -> str | None:
 
 __all__ = [
     "BaseGGUFWeightsAdapter",
+    "DeepseekV41GGUFAdapter",
     "DiffusionGGUFAdapter",
     "Flux2KleinDiffusionGGUFAdapter",
     "GGUFModelFiles",
diff --git a/vllm_gguf_plugin/weights_adapter/deepseek_v41.py b/vllm_gguf_plugin/weights_adapter/deepseek_v41.py
new file mode 100644
index 0000000..5a14847
--- /dev/null
+++ b/vllm_gguf_plugin/weights_adapter/deepseek_v41.py
@@ -0,0 +1,133 @@
+# SPDX-License-Identifier: Apache-2.0
+"""GGUF weight adapter for DeepSeek V4.1 text-backbone artifacts.
+
+The current V4.1 GGUF converter emits compact llama.cpp tensor names while the
+vLLM ROCm model loader consumes the native DeepSeek checkpoint namespace.  Keep
+this translation explicit and fail closed: the quantized artifact intentionally
+omits Engram/MTP, which are supplied by separate DS41 runtime components.
+"""
+
+from __future__ import annotations
+
+import os
+import re
+from collections.abc import Iterable
+
+from vllm.logger import init_logger
+
+from ..gguf_files import GGUFModelFiles
+from ..weight_utils import get_gguf_tensor_names
+from .base import BaseGGUFWeightsAdapter, GGUFWeight
+
+logger = init_logger(__name__)
+
+_TOP = {
+    "token_embd": "embed.weight",
+    "output_norm": "norm.weight",
+    "output": "head.weight",
+}
+
+_LAYER = {
+    "attn_kv": "attn.wkv.weight",
+    "attn_kv_a_norm": "attn.kv_norm.weight",
+    "attn_norm": "attn_norm.weight",
+    "attn_output_a": "attn.wo_a.weight",
+    "attn_output_b": "attn.wo_b.weight",
+    "attn_q_a": "attn.wq_a.weight",
+    "attn_q_a_norm": "attn.q_norm.weight",
+    "attn_q_b": "attn.wq_b.weight",
+    "attn_sinks": "attn.attn_sink",
+    "attn_compressor_gate": "attn.compressor.wgate.weight",
+    "attn_compressor_kv": "attn.compressor.wkv.weight",
+    "attn_compressor_norm": "attn.compressor.norm.weight",
+    "indexer.attn_k": "attn.indexer.wk.weight",
+    "indexer.attn_q_b": "attn.indexer.wq_b.weight",
+    "indexer.k_norm": "attn.indexer.k_norm.weight",
+    "indexer.proj": "attn.indexer.weights_proj.weight",
+    "ffn_down_exps": "ffn.experts.0.w2.weight",
+    "ffn_gate_exps": "ffn.experts.0.w1.weight",
+    "ffn_up_exps": "ffn.experts.0.w3.weight",
+    "ffn_down_shexp": "ffn.shared_experts.w2.weight",
+    "ffn_gate_shexp": "ffn.shared_experts.w1.weight",
+    "ffn_up_shexp": "ffn.shared_experts.w3.weight",
+    "ffn_gate_inp": "ffn.gate.weight",
+    "exp_probs_b": "ffn.gate.bias",
+    "exp_probs_b_vl": "ffn.gate.bias_vl",
+    "ffn_norm": "ffn_norm.weight",
+    "hc_attn_base": "hc_attn_base",
+    "hc_attn_fn": "hc_attn_fn",
+    "hc_attn_scale": "hc_attn_scale",
+    "hc_ffn_base": "hc_ffn_base",
+    "hc_ffn_fn": "hc_ffn_fn",
+    "hc_ffn_scale": "hc_ffn_scale",
+}
+
+_BLOCK_RE = re.compile(r"^blk\.(\d+)\.(.+)$")
+
+
+def map_deepseek_v41_gguf_name(name: str) -> str | None:
+    if name in _TOP:
+        return _TOP[name]
+    match = _BLOCK_RE.fullmatch(name)
+    if match is None:
+        return None
+    layer, leaf = match.groups()
+    target = _LAYER.get(leaf)
+    if target is None:
+        return None
+    return f"layers.{layer}.{target}"
+
+
+class DeepseekV41GGUFAdapter(BaseGGUFWeightsAdapter):
+    """Map llama.cpp DeepSeek4/V4.1 GGUF tensors to native V4.1 names."""
+
+    @classmethod
+    def matches(cls, config) -> bool:
+        return getattr(config, "model_type", None) == "deepseek_v41"
+
+    @classmethod
+    def architecture(cls, config) -> str | None:
+        if not cls.matches(config):
+            return None
+        return "DeepseekV41ForCausalLM"
+
+    def build_name_map(self, files: GGUFModelFiles, model_config) -> dict[str, str]:
+        del model_config
+        names = sorted(get_gguf_tensor_names(files.backbone))
+        mapped: dict[str, str] = {}
+        unmapped: list[str] = []
+        for name in names:
+            target = map_deepseek_v41_gguf_name(name)
+            if target is None:
+                unmapped.append(name)
+            else:
+                mapped[name] = target
+        if unmapped:
+            raise RuntimeError(
+                f"Failed to map {len(unmapped)} DeepSeek V4.1 GGUF tensors: {unmapped}"
+            )
+        logger.info("Mapped all %d DeepSeek V4.1 GGUF tensors", len(mapped))
+        return mapped
+
+    def transform_weights(
+        self, weights: Iterable[GGUFWeight], model_config
+    ) -> Iterable[GGUFWeight]:
+        del model_config
+        phase_log = os.environ.get("DS41_LOAD_PHASE_LOG", "0") == "1"
+        for name, weight in weights:
+            if weight.ndim == 3 and ".experts.0." in name:
+                if phase_log:
+                    logger.info(
+                        "DS41_LOAD expert_tensor begin name=%s shape=%s",
+                        name,
+                        tuple(weight.shape),
+                    )
+                for expert_id, expert_weight in enumerate(weight.unbind()):
+                    expert_name = name.replace(
+                        ".experts.0.", f".experts.{expert_id}."
+                    )
+                    yield expert_name, expert_weight
+                if phase_log:
+                    logger.info("DS41_LOAD expert_tensor end name=%s", name)
+            else:
+                yield name, weight
