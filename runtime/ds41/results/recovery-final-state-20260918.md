# DS41 Recovery final state — 2026-09-18

Recovery delivery is terminal. DS4 R4 materially recovered the non-ambiguous retrieval failures but did not pass the complete frozen quality panel, so no performance arm or promotion was admitted.

Operational rollback is restored: `k2-prefill-5bdfed6`, epoch `1789712415010337206`, READY with rank0/rank1/paired HTTP200. Gateway `/health` HTTP200. Final non-reasoning arithmetic smoke returned exactly `323` with natural `stop`.

Antirez Q2 is full-size/SHA verified on both nodes. No DS4 unit remains intentionally promoted.
