# DS41 RECOVERY R2 — window-QAT differential preregister

Frozen before execution. Only the128 SWA rows change; upstream sparse-attention `0.02/0.02` gate decides component-level materiality. No correctness claim from either outcome.
